import { FilProductPipe } from './fil-product.pipe';

describe('FilProductPipe', () => {
  it('create an instance', () => {
    const pipe = new FilProductPipe();
    expect(pipe).toBeTruthy();
  });
});
